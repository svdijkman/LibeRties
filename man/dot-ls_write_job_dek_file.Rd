% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jobs.R
\name{.ls_write_job_dek_file}
\alias{.ls_write_job_dek_file}
\title{Write the per-job DEK to a short-lived key file (0600) and return its path.
The path (not the secret) is passed to the worker via LIBERTIES_JOB_KEY_PATH.}
\usage{
.ls_write_job_dek_file(job_path, dek_hex)
}
\description{
Write the per-job DEK to a short-lived key file (0600) and return its path.
The path (not the secret) is passed to the worker via LIBERTIES_JOB_KEY_PATH.
}
\keyword{internal}
